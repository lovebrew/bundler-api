[package]
name = "asset"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow.workspace = true
image = { version = "0.25.6", features = ["png", "jpeg"] }
ttf-parser = "0.25.1"

system = { path = "../system" }
